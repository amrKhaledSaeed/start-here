<x-layouts.store>
    <div class="space-y-2">
        <h1 class="text-2xl font-semibold text-zinc-900 dark:text-zinc-100">{{ __('Product Details') }}</h1>
        <p class="text-zinc-600 dark:text-zinc-300">
            {{ __('Product slug') }}: <span class="font-mono">{{ $slug }}</span>
        </p>
        <p class="text-zinc-600 dark:text-zinc-300">
            {{ __('Product detail page placeholder. Implementation starts in the next sprint tasks.') }}
        </p>
    </div>
</x-layouts.store>
